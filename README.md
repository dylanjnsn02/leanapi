# leanapi
A terminal-based HTTP API client (like Postman, but in your terminal) — built in Go with Bubble Tea. Mouse-clickable UI for building requests with query params, headers, cookies, auth (Basic/Bearer/API Key), and a body editor, plus pretty-printed JSON responses and persistent request history.
